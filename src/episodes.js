export function addEpisode(seriesId, seasonId) {
    return true;
}

export function findEpisode(seriesId, seasonId, episodeId) {
    return { msg: "Hot stuff" };
}

export function deleteEpisode(seriesId, seasonId, episodeId) {
    return true;
}